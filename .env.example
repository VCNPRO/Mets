# Database (auto-configuradas por Neon en Vercel)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Authentication (CRÍTICO - genera con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=

# Rate Limiting (opcional pero recomendado)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Notificaciones Email (opcional)
RESEND_API_KEY=
ADMIN_EMAIL=

# Protección de Cron Jobs
CRON_SECRET=
